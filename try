ठीक आहे! मग folder नको, direct Components मध्येच फाईल तयार कर!
हे absolutely fine आहे.


---

✅ Final Instruction (तुझ्या साठी perfect setup)

📍 इथे फाईल तयार कर:

src/Components/CalculatePremium.tsx

बस्स! No folder needed.


---

🚀 FULL WORKING FILE CODE (direct paste करण्यासाठी तयार)

📄 src/Components/CalculatePremium.tsx

import React, { useState } from "react";
import axios from "../api/axiosInterceptor";

const CalculatePremium: React.FC = () => {
  const [sumAssured, setSumAssured] = useState("");
  const [age, setAge] = useState("");
  const [policyTerm, setPolicyTerm] = useState("");
  const [premium, setPremium] = useState<number | null>(null);
  const [message, setMessage] = useState("");

  const handleCalculate = async () => {
    try {
      const payload = {
        sumAssured: Number(sumAssured),
        age: Number(age),
        policyTerm: Number(policyTerm),
      };

      const res = await axios.post("/premium/calculate", payload);

      setPremium(res.data.premium);
      setMessage(res.data.message);
    } catch (error) {
      console.error("Error:", error);
      setMessage("Error calculating premium!");
    }
  };

  return (
    <div style={{ maxWidth: "450px", margin: "30px auto", padding: "20px", border: "1px solid #ddd", borderRadius: "10px" }}>
      <h2 style={{ textAlign: "center" }}>Premium Calculator</h2>

      <label>Sum Assured</label>
      <input
        type="number"
        value={sumAssured}
        onChange={(e) => setSumAssured(e.target.value)}
        placeholder="Enter Sum Assured"
        style={{ width: "100%", padding: "10px", marginBottom: "10px" }}
      />

      <label>Age</label>
      <input
        type="number"
        value={age}
        onChange={(e) => setAge(e.target.value)}
        placeholder="Enter Age"
        style={{ width: "100%", padding: "10px", marginBottom: "10px" }}
      />

      <label>Policy Term</label>
      <input
        type="number"
        value={policyTerm}
        onChange={(e) => setPolicyTerm(e.target.value)}
        placeholder="Enter Policy Term"
        style={{ width: "100%", padding: "10px", marginBottom: "15px" }}
      />

      <button
        onClick={handleCalculate}
        style={{
          width: "100%",
          padding: "12px",
          backgroundColor: "black",
          color: "white",
          border: "none",
          borderRadius: "5px",
          cursor: "pointer",
        }}
      >
        Calculate Premium
      </button>

      {premium !== null && (
        <div style={{ marginTop: "20px", padding: "15px", backgroundColor: "#f4f4f4", borderRadius: "5px" }}>
          <h3>Premium: ₹{premium}</h3>
          <p>{message}</p>
        </div>
      )}
    </div>
  );
};

export default CalculatePremium;


---

✔ Step 2 → App.tsx मध्ये route जोडा

src/App.tsx उघड
Route add कर:

import CalculatePremium from "./Components/CalculatePremium";

<Route path="/calculate-premium" element={<CalculatePremium />} />


---

✔ Step 3 → Menu / Tabs मध्ये link add कर

<Link to="/calculate-premium">Calculate Premium</Link>


---

🎉 तुझा Frontend Premium Calculator पूर्ण!

✔ Backend ला request जाईल
✔ Premium UI वर दिसेल
✔ कोणताही error नाही
✔ तुझ्या project structure मध्ये perfect बसतो


---

जर तुला हवं असेल तर:

🔥 मी UI अजून attractive करुन देऊ
🔥 Validations add करुन देऊ
🔥 Values auto-fill करु शकतो (PlanAndCoverage मधून)

फक्त सांग — पुढे काय हवय?
